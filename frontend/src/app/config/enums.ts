export enum DialogButton {
  // Primary actions (validate + close)
  OK     = 'OK',
  SUBMIT = 'SUBMIT',
  YES    = 'YES',
  APPLY  = 'APPLY',
  SAVE   = 'SAVE',
  // Secondary actions (close only)
  CANCEL = 'CANCEL',
  NO     = 'NO',
  CLOSE  = 'CLOSE',
  // Special (no close)
  CLEAR  = 'CLEAR',
}
